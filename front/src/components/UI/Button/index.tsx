import { useRouter } from 'next/router'

interface ReportControlProps {
    reports: Array<any>
  }
  
  const Button = ({ reports }: ReportControlProps) => {
    return (
      <>
      </>
    )
  }
  
  export default Button